version: '3.8'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                - VITE_BINDUK_APP_URL=${VITE_BINDUK_APP_URL:-http://localhost}
        container_name: arsip-smk-mandiri-app
        restart: always
        environment:
            DB_CONNECTION: pgsql
            DB_HOST: global-postgres-db
            DB_PORT: 5432
            DB_DATABASE: ${DB_DATABASE:-arsip_smk_mandiri_prod}
            DB_USERNAME: ${DB_USERNAME:-postgres}
            DB_PASSWORD: ${DB_PASSWORD:-root}
            REDIS_HOST: redis
        volumes:
            - public_data_prod:/var/www/public_shared
            - storage_data_prod:/var/www/storage
        command: >
            sh -c "cp -a /var/www/public/. /var/www/public_shared/ && 
                   chown -R www-data:www-data /var/www/public_shared /var/www/storage && 
                   chmod -R 775 /var/www/public_shared /var/www/storage && 
                   php-fpm"
        networks:
            - global-vps-network

    queue-worker:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: arsip-smk-mandiri-worker
        restart: always
        environment:
            DB_CONNECTION: pgsql
            DB_HOST: global-postgres-db
            DB_PORT: 5432
            DB_DATABASE: ${DB_DATABASE:-arsip_smk_mandiri_prod}
            DB_USERNAME: ${DB_USERNAME:-postgres}
            DB_PASSWORD: ${DB_PASSWORD:-root}
            REDIS_HOST: redis
        volumes:
            - public_data_prod:/var/www/public_shared
            - storage_data_prod:/var/www/storage
        command: php artisan queue:work --verbose --tries=3
        networks:
            - global-vps-network
        depends_on:
            - app

    webserver:
        image: nginx:alpine
        container_name: arsip-smk-mandiri-nginx
        restart: always
        ports:
            - '8080:80'
        volumes:
            - public_data_prod:/var/www/public
            - storage_data_prod:/var/www/storage
            - ./docker-config/app.conf:/etc/nginx/conf.d/default.conf
        networks:
            - global-vps-network
        depends_on:
            - app

volumes:
    public_data_prod:
    storage_data_prod:

networks:
    global-vps-network:
        external: true
